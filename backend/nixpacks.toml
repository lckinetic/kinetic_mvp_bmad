[phases.install]
cmds = ["/mise/installs/python/3.11.9/bin/pip install -r requirements.txt"]

[start]
cmd = "/mise/installs/python/3.11.9/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"